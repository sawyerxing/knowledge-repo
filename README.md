# Knowledge Model Fine-Tuning

This repository includes scripts and templates for fine-tuning a machine learning model.

## Make Commands

- `make preprocess` - Run data preprocessing.
- `make train` - Train the model.
- `make test` - Test the model.